# v0.5.9 — GUI 同步与导入保护

- GUI 提供检查更新、上传改动、拉取并应用；上传自动记录插件，拉取自动安装，无需单独记录或安装。
- 缺失插件清单时停止安装和卸载；相同精确来源及版本跳过重复安装。
- 插件安装明确使用目标 DSH Home。
- 应用前校验配置文件并解密凭据；缺失密钥不会生成替代密钥，未包含的残留密文不恢复。
- 本机 overlay 字段不再进入导出 settings；配置写入失败尝试恢复本次已写入文件。
- 上传在导出环境前检查插件来源，避免本地 link 来源错误造成部分配置导出。

## 验证与范围

23 项测试、JavaScript 语法检查、打包预检及差异检查通过。发布包含运行所需 lib 与 scripts。

本版本未实现多电脑配置冲突合并、跨进程崩溃恢复、整套插件安装回滚或管理器自更新。仍需将本机 link 插件迁移为固定 Git 来源；私有配置仓库必须已具备有效清单，本机专用字段需登记在 overlay 中。没有进行真实第二台电脑验收。
