# 0.6.1

首次安装生成独立的 Start DSH.vbs 与 start-dsh.ps1，保存本机启动路径。重启电脑后无需使用旧聚合包脚本或重新配置环境变量即可启动。原有环境的开机入口应改指向这个启动器。

验证：PowerShell 语法解析、30 项同步测试与打包检查；同步执行代码与已通过实际 GUI 验收的 0.6.0 相同。

# 0.6.0

整套 Git 插件与共享配置同步正式版。上传与拉取包含新增、删除、固定版本更新、启停和配置；配置预检、Git 冲突选择、失败恢复、管理器自更新及 Windows GUI 重启使用同一入口。

兼容验收基于干净的官方 DSH 提交 d347e703908d0406b7a7ef80e3a0e594d86b2215。30 项测试、真实双 Home 上传与拉取、实际插件增删及失败回退、首次安装、22 插件启动、含空格路径安装和 GUI 重启后实际进程切换通过。目标电脑的设备地址、资料库挂载和模型服务仍需在首次安装时验证。

迁移要求：正式共享插件使用固定 Git 提交；旧聚合包不再安装；本机开发目录保留，不作为恢复来源。同步密钥在仓库外分发，旧仓库中的密钥需备份后轮换，历史密文仍按旧密钥的访问范围保护。

# 0.6.0-rc.7

Windows 安装通过 cross-spawn 处理带空格的源码与用户目录；重启使用原生绝对路径与隐藏的独立 PowerShell 进程，保留启动和执行日志。

验证：30 项测试、语法检查、带空格目录的真实插件安装通过。GUI 进程切换继续验收。

# 0.6.0-rc.6

报告管理器实际加载版本和内容摘要，区分已安装与运行中版本。GUI 重启使用独立本机脚本并保留重启日志，不依赖更新前的包目录；显式请求重启。

验证：继续执行自更新后的实际进程切换验收。

# 0.6.0-rc.5

GUI 单独安装和卸载也保存完整环境恢复点；恢复不依赖配置仓库已设置。迁移本地开发链接时先移动链接本身，保护源码目录免受安装写入。

验证：干净官方 d347e703908d0406b7a7ef80e3a0e594d86b2215 构建、首次安装、22 插件启动、GUI 新增和卸载通过；真实本地链接转 Git 安装后源码校验未变化。

# 0.6.0-rc.4

修复环境应用与插件安装嵌套锁的释放冲突，修正 pnpm remove 的解析参数；启停状态采用实际补丁值，已有 profile 配置可以保留并通过管理区块调整启停。

验证：真实双环境 Git 上传/拉取、共享设置、删除与新增插件、用户数据保留、安装后失败回退及重试通过；增加独立锁释放与补丁状态回归测试。干净官方源码与首次安装验收仍在执行。

# 0.6.0-rc.3

插件变更先在独立解析目录生成锁文件，再通过官方入口统一安装，避免 pnpm 11 将已安装源码链接误解析为未发布的注册表版本。首次安装复用同一顺序；GUI 保存密钥前验证现有加密快照。

验证：27 项测试、语法检查通过；22 插件环境中管理器固定 Git 更新、重启与实际管理页面加载通过。整套双环境验收仍在执行。

# 0.6.0-rc.2

移除管理器未使用、已拆分的 `dsh-client-runtime` 注入与 peer 声明，避免源码启动环境更新时解析旧接口包。完整部署验收仍在执行。

# 0.6.0-rc.1

候选版本，继续进行整套部署验收。

- 将上传和拉取合并为保存本机修改、Git 合并、配置与插件应用、上传的可恢复流程；增加冲突 GUI 与失败重试。
- 将管理器自身纳入清单，增加 GUI 安装/更新、卸载、密钥设置、环境恢复和重启。
- 提交预检、插件删除基线、跨进程互斥、持久化插件与配置回退记录。
- 凭据格式覆盖订阅账户，并明确删除；隔离 NAS、情感资料配置与 Rabi Manager 地址，保留本机 webserver 配置。
- 源码启动的官方依赖统一绑定到本机 DSH 源码，防止旧 npm 接口导致启动失败。
- 首次安装脚本保留独立恢复运行时和 Windows 恢复入口。

验证：27 项测试及语法检查通过；现有 22 个目标插件的固定 Git 安装和隔离启动检查通过。候选管理器的完整 GUI、自更新与双环境安装验收仍在执行。

# 0.5.9

增加 GUI 检查更新和上传/拉取主操作；缺失清单停止、配置预检、凭据解密前置、本机 overlay 字段剔除。此版本没有完整环境事务、冲突 GUI 或管理器自更新。
