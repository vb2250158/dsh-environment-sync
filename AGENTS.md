# AGENTS.md

- 本仓库只保存插件管理器的公开源码、构建产物、测试、文档和数据格式。
- 用户安装清单、启停状态、配置、凭据和业务数据只进入独立私有配置仓库。
- 私有仓库必须位于本仓库之外；凭据必须加密；同步密钥不得进入 Git。
- 所有公开插件通过独立仓库和固定提交安装；安装必须调用官方 `dsh plugin` 入口。
- 插件通过 `dsh.bundle`、Remote 和 settings slot 接入 DSH，不修改官方源码。
- 发布前运行测试、语法检查、`npm pack --dry-run` 和敏感信息扫描。
